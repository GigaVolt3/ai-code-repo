# Documentation - HF-006 Model Inference

The lesson is authored as a **Jupyter notebook** (per project convention) so
it doubles as an interactive walkthrough.

| File | Description |
|------|-------------|
| [`06_Model_Inference.ipynb`](06_Model_Inference.ipynb) | Full beginner lesson: tokenize-forward-softmax-argmax by hand, logits vs probabilities, pipeline equality |

## Open the notebook

```bash
pip install jupyter
jupyter lab documentation/06_Model_Inference.ipynb
```

(Or open it directly in VS Code, which renders `.ipynb` natively.)

## Companion files

- Lesson script: `../06_Model_Inference.py` (same content, runnable without Jupyter)
- Tests: `../test/`
- Extra reading: `../resources/reference_links.md`
