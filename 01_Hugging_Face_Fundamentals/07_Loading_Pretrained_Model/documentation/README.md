# Documentation - HF-007 Loading Pretrained Models

The lesson is authored as a **Jupyter notebook** (per project convention) so
it doubles as an interactive walkthrough.

| File | Description |
|------|-------------|
| [`07_Loading_Pretrained_Model.ipynb`](07_Loading_Pretrained_Model.ipynb) | Full beginner lesson: Hub / local folder / revision sources, snapshot_download, save/reload, offline mode |

## Open the notebook

```bash
pip install jupyter
jupyter lab documentation/07_Loading_Pretrained_Model.ipynb
```

(Or open it directly in VS Code, which renders `.ipynb` natively.)

## Companion files

- Lesson script: `../07_Loading_Pretrained_Model.py` (same content, runnable without Jupyter)
- Tests: `../test/`
- Extra reading: `../resources/reference_links.md`
