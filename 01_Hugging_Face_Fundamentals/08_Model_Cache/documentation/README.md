# Documentation - HF-008 Model Cache

The lesson is authored as a **Jupyter notebook** (per project convention) so
it doubles as an interactive walkthrough.

| File | Description |
|------|-------------|
| [`08_Model_Cache.ipynb`](08_Model_Cache.ipynb) | Full beginner lesson: cache layout, scan_cache_dir, delete_revisions, HF_HOME, offline mode |

## Open the notebook

```bash
pip install jupyter
jupyter lab documentation/08_Model_Cache.ipynb
```

(Or open it directly in VS Code, which renders `.ipynb` natively.)

## Companion files

- Lesson script: `../08_Model_Cache.py` (same content, runnable without Jupyter)
- Tests: `../test/`
- Extra reading: `../resources/reference_links.md`
