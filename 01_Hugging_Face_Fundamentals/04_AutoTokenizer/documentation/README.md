# Documentation - HF-004 AutoTokenizer

The lesson is authored as a **Jupyter notebook** (per project convention) so
it doubles as an interactive walkthrough.

| File | Description |
|------|-------------|
| [`04_AutoTokenizer.ipynb`](04_AutoTokenizer.ipynb) | Full beginner lesson: BERT vs GPT-2 vs T5 tokenizer families, padding/truncation, attention_mask, save/reload |

## Open the notebook

```bash
pip install jupyter
jupyter lab documentation/04_AutoTokenizer.ipynb
```

(Or open it directly in VS Code, which renders `.ipynb` natively.)

## Companion files

- Lesson script: `../04_AutoTokenizer.py` (same content, runnable without Jupyter)
- Tests: `../test/`
- Extra reading: `../resources/reference_links.md`
