# Documentation - HF-005 AutoModel

The lesson is authored as a **Jupyter notebook** (per project convention) so
it doubles as an interactive walkthrough.

| File | Description |
|------|-------------|
| [`05_AutoModel.ipynb`](05_AutoModel.ipynb) | Full beginner lesson: config blueprint, forward pass shapes, task heads, parameter counting |

## Open the notebook

```bash
pip install jupyter
jupyter lab documentation/05_AutoModel.ipynb
```

(Or open it directly in VS Code, which renders `.ipynb` natively.)

## Companion files

- Lesson script: `../05_AutoModel.py` (same content, runnable without Jupyter)
- Tests: `../test/`
- Extra reading: `../resources/reference_links.md`
