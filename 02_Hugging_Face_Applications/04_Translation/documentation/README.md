# Documentation — HF-204 Translation

The lesson is authored as a **Jupyter notebook** (per project convention) so
it doubles as an interactive walkthrough.

| File | Description |
|------|-------------|
| [`04_Translation.ipynb`](04_Translation.ipynb) | Full beginner lesson: machine translation, one model per language pair, tokenize → generate → decode, batch translation, round-trip test, exercises |

## Open the notebook

```bash
pip install jupyter
jupyter lab documentation/04_Translation.ipynb
```

(Or open it directly in VS Code, which renders `.ipynb` natively.)

## Companion files

- Lesson script: `../04_Translation.py` (same content, runnable without Jupyter)
- Tests: `../test/`
- Extra reading: `../resources/reference_links.md`