# Documentation — HF-202 Summarization

The lesson is authored as a **Jupyter notebook** (per project convention) so
it doubles as an interactive walkthrough.

| File | Description |
|------|-------------|
| [`02_Summarization.ipynb`](02_Summarization.ipynb) | Full beginner lesson: what summarization is, the seq2seq idea, tokenize → generate → decode, beam search, chunking long documents, exercises |

## Open the notebook

```bash
pip install jupyter
jupyter lab documentation/02_Summarization.ipynb
```

(Or open it directly in VS Code, which renders `.ipynb` natively.)

## Companion files

- Lesson script: `../02_Summarization.py` (same content, runnable without Jupyter)
- Tests: `../test/`
- Extra reading: `../resources/reference_links.md`